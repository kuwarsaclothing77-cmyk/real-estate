# Branding

To change the website logo, paste the image or SVG URL in `brand-config.js` as the `logoUrl` value.

Alternatively, replace `logo.svg` with your own SVG file. The website uses it automatically when `logoUrl` is empty.
